specVersion: "v1"
serviceId: "smartfabapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smartfabapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
